# secrets
.env

# runtime logs
*.log

# watchdog runtime state (which engine to auto-restart)
profiles/watchdog/.last-engine
profiles/watchdog/.idle-state

# python bytecode (dashboard.py runs in place)
__pycache__/
*.pyc

# ad-hoc scratch exports
csv

# weights and JIT caches live under _archive/ and are ignored by _archive/.gitignore

# local claude session config / runtime state (any level)
.claude/

# macOS
.DS_Store